import 'package:flutter/material.dart';

class CutomPrimaryButton extends StatelessWidget {
  const CutomPrimaryButton({super.key});

  @override
  Widget build(BuildContext context) {
    return ElevatedButton(onPressed: () {}, child: Text("Login"));
  }
}
